#include <stdio.h>
/**
 * main - Entry point
 * Description: This prints all single digits number of base 10
 * starting from 0 using one putchar and without declaring
 * using char
 * Return: 0
 */

int main(void)
{
	int a;

	for (i = 0; i < 10, i++)
	{
		putchar(i + '0');
	}
	putchar('\n');
	return (0);
}
